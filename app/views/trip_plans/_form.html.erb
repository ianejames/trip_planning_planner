<%= form_for(@trip_plan, remote: true) do |f| %>
  <% if @trip_plan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trip_plan.errors.count, "error") %> prohibited this trip_plan from being saved:</h2>

      <ul>
      <% @trip_plan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, placeholder: "Example: San Francisco Trip" %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
