<% content_for :header_accessory do %>
  <%= link_to 'New Trip plan', new_trip_plan_path, remote: true %>
<% end %>

<p id="notice"><%= notice %></p>

<h1>Listing Trip Plans</h1>

<ol class="tripPlans">
  <% @trip_plans.each do |trip_plan| %>
    <%= render trip_plan %>
  <% end %>
</ol>
